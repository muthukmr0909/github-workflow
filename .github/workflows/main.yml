name: staging deployment

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: windows-latest
    steps: 
      - name: print hello world
        run: echo "hello world"

      - name: Deploy code to Lucee server
        run: |
          # Use your deployment commands here
          # Example: Copy files to the server, trigger a deployment script, etc.
          echo "Deploying Lucee code to server"
          # Example SCP command for copying files to the server
          scp -r ./* 118@192.168.1.52:/workflow
          
          # REM Running deployment script on Windows
          deployment.bat

name: staging deployment

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: windows-latest
    steps: 
      - name: print hellow world
        run: echo "hello world"

      
















          
